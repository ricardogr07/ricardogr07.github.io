// Custom Cypress commands. Add project-specific commands here.
// See: https://on.cypress.io/custom-commands

export {}

declare global {
  // eslint-disable-next-line @typescript-eslint/no-namespace
  namespace Cypress {
    interface Chainable {
      // Add custom command types here
    }
  }
}
